# Pokédex — Audit TODO

Generated from a full repo audit on 2026-09-22. Organized by category, most impactful first. See also [FUTURE_IDEAS.md](FUTURE_IDEAS.md).

## 🐛 Bugs / Errors

- [x] **`npm audit` reports 14 vulnerabilities (1 critical, 9 high, 3 moderate, 1 low)** — ran `npm audit fix`; all 14 were fixable within the existing `^` semver ranges in `package.json` (no `--force`/major bumps needed). Lockfile now pulls `axios@1.20.0`, `vite@7.3.6`, `vitest@4.1.11`, etc. `npm audit` now reports 0 vulnerabilities. Verified `eslint`/`vite build` still pass after the bump.
- [x] **Stray `.next/` directory** at repo root (untracked) — checked: it's no longer present in the working tree (was only in the initial session snapshot, not reproduced by any script in this repo), so nothing to delete. No `.gitignore` entry added since it isn't being regenerated — revisit if it reappears.
- [x] **No LICENSE file** despite `README.md` linking to `[LICENSE](LICENSE)` and claiming MIT — added `LICENSE` (MIT, copyright barkerbg001, 2026).
- [x] **Pagination silently stalls on API failure after the first page** — was: the mock-data fallback only triggered when `offset === 0`, and `hasMore` was never reset on a failed later page, so the `IntersectionObserver` retried forever. Fixed incidentally by the generation-sidebar rework: `loadGenerationBatch`'s catch block now unconditionally sets `hasMore(false)` on any failure, not just the first batch. Still no user-visible error message or retry button — that part of this item is now tracked separately below.
- [x] **No request cancellation on unmount** — `axios.get` calls in [Pokedex.js](src/components/Pokedex/Pokedex.js) and [PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js) aren't aborted when the component unmounts (e.g. closing the modal mid-fetch), risking "set state on unmounted component" issues and wasted network calls. Use `AbortController` or an `isMounted`/cleanup guard. — Used cleanup guards: PokemonDetail's evolution/forms and effectiveness effects, its form picker, and the search lookup in Pokedex now ignore responses that arrive after they've been superseded. The real bug was stale data (a slow response for the previous Pokémon/form overwriting the new one, or an old search clearing the "Searching…" indicator early); React 18 no longer warns about unmounted setState. Requests aren't aborted because [pokeapi.js](src/api/pokeapi.js) shares in-flight requests between callers (aborting would fail them all) and caches the results, so they aren't wasted. Pokedex's other one-off fetches are left as is, since Pokedex never unmounts.
- [x] **`String.replace('-', ' ')` only replaces the first hyphen** ([PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js)) — stat/move/ability names with multiple hyphens weren't fully formatted. Fixed: all five uses (name, stats, abilities, moves, learn method) now use `.replaceAll('-', ' ')`; PokemonDetail was the only file doing this.
- [x] **Unsafe array access in moves tab** ([PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js)): `m.version_group_details[0].move_learn_method.name` would throw if `version_group_details` was empty. Fixed: the learn-method label is only rendered when a first entry exists.
- [x] **Fragile modal backdrop-click check** ([PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js)): compared `e.target.className === 'modal'`. Already resolved: PokemonDetail now renders inside the shared [Modal.js](src/components/Modal/Modal.js), whose backdrop handler compares `e.target === e.currentTarget`; the old check no longer exists anywhere in `src/`.
- [x] **`theme` prop is passed to `Pokedex` but never used** — now actually used: the theme toggle moved out of the navbar into a new Settings view (reachable from the Menu), and `theme`/`onToggleTheme` are passed from `App.js` through `Pokedex.js` to `Settings.js`.
- [x] **Favorites can silently disappear** — was: a favorited Pokémon only rendered in the favorites strip if it happened to already be loaded. Fixed as part of the generation-sidebar rework below: a dedicated effect now resolves any favorited id not present in the currently loaded list via a direct `/pokemon/{id}` fetch, so favorites show up regardless of which generation is selected or whether that Pokémon was ever paginated in.
- [x] ~~Search only applies to already-loaded Pokémon~~ — fixed: search now checks a full name index and fetches missing matches from PokéAPI on demand (see [Server-side/API-backed search](#-additional-feature-ideas-not-yet-in-readme) below). The **type filter** is still limited to loaded Pokémon only.
- [x] ~~No user-visible indicator when the app has silently fallen back to mock/demo data after an API failure~~ — moot: the mock/demo dataset has been removed entirely (see below). Failures now show a visible error state instead of silently substituting fake data.
- [x] **`PokemonDetail.js` evolution/forms fetch was silently broken for every real (non-mock) Pokémon**: [Pokedex.js](src/components/Pokedex/Pokedex.js) overwrites `pokemon.species` with the fully-expanded species resource (for generation grouping), but a species resource has no self `.url` field. `PokemonDetail.js` was doing `axios.get(pokemon.species.url)` → `axios.get(undefined)`, which always failed and silently fell into the mock-data catch block — meaning the Evolution tab (and now Forms) never used real data for anything loaded from the live API, only ever the 3 hardcoded mock Pokémon. Fixed by reusing the already-expanded `species` object when present (`pokemon.species.evolution_chain` exists) and only fetching by URL for the bare `{name, url}` reference shape (mock data). Bonus: this also removes a redundant species re-fetch for every real Pokémon's detail view.

## 🔒 Security

- [x] ~~Address the `npm audit` findings above~~ — see the `npm audit fix` entry above; 0 vulnerabilities remaining.
- [ ] Set up Dependabot **security** updates (currently only version-update schedule is configured in [dependabot.yml](.github/dependabot.yml)) or enable GitHub's Dependabot alerts. Both are repo settings, not `dependabot.yml` options: **Settings → Code security → Dependabot alerts / Dependabot security updates → Enable**, or `gh api -X PUT repos/barkerbg001/Pokedex/vulnerability-alerts && gh api -X PUT repos/barkerbg001/Pokedex/automated-security-fixes` with an admin token (the Codespace token gets a 403).

## 🧹 Code Quality / Tech Debt

- [x] **Duplicated `typeColors`/`getTypeColor` maps** in both [Pokedex.js](src/components/Pokedex/Pokedex.js) and [PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js) — extracted to [src/constants.js](src/constants.js) (`typeColors`, `getTypeColor`, `generationNames`) and imported from both files.
- [x] **No ESLint or Prettier configuration** anywhere in the repo — added [eslint.config.js](eslint.config.js) (flat config, React + hooks + refresh plugins, Prettier-compatible) and [.prettierrc.json](.prettierrc.json)/[.prettierignore](.prettierignore), plus `npm run lint`, `npm run format`, `npm run format:check` scripts. Fixed the resulting lint findings (unused catch bindings, one empty catch block). Note: only the files touched this session were run through `prettier --write`; the rest of the repo has never been formatted — run `npm run format` repo-wide when it's a good time for a large, low-risk diff.
- [x] **No TypeScript**, despite `@types/react`/`@types/react-dom` being installed as devDependencies with no `.ts`/`.tsx` files anywhere — either remove the unused type packages or consider migrating for type safety on API response shapes. — Removed the unused `@types/react`/`@types/react-dom` packages; staying on plain JS for now.
- [x] Leftover `console.log`/debug logging in production code paths (API-failure handlers in [Pokedex.js](src/components/Pokedex/Pokedex.js), [PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js), [registerSW.js](src/registerSW.js#L7), [InstallPrompt.js](src/components/InstallPrompt/InstallPrompt.js)) — gate behind a dev flag or remove. Removed the "SW registered" success log; the failure messages now go through `devWarn` in [logger.js](src/logger.js), which only prints in development.
- [x] Hardcoded PokéAPI base URL repeated across files — centralized into [src/api/pokeapi.js](src/api/pokeapi.js), a small `axios`-based client with `baseURL` set once. Every `axios.get` call across `Pokedex.js` and `PokemonDetail.js` now goes through `pokeapi.get(...)` (it accepts either a relative path or a full URL, so calls using API-provided `.url` references didn't need rewriting). Also added, since PokéAPI resources are immutable: in-memory response caching (keyed by URL, indefinite TTL) that also dedupes concurrent identical requests, and automatic retry with backoff (2 retries) on failed requests, without caching failures. This directly helps the "no response cache" performance item below.
- [x] Dead/half-dead branches in [reportWebVitals.js](src/reportWebVitals.js): `getCLS`/`getFID`/`getFCP`/`getLCP`/`getTTFB` no longer exist on `web-vitals@5.x`, so half of every `if/else` was always false. Fixed: it now calls only the `on*` API (`onCLS`/`onINP`/`onFCP`/`onLCP`/`onTTFB`).
- [x] `site.webmanifest` and `manifest.json` in [public/](public/) were duplicates; only `manifest.json` is referenced (from [index.html](index.html#L15) and [sw.js](public/sw.js)). Fixed: removed `site.webmanifest`, a less complete leftover copy that nothing linked to (Safari pinned tabs use `<link rel="mask-icon">`, not a manifest), and dropped it from the README's project tree.
- [x] README's **Project Structure** section still describes `public/index.html` as the HTML template — fixed to show `index.html` at the repo root, and added the files introduced since the audit (`constants.js`, `eslint.config.js`, `.prettierrc.json`, `LICENSE`, `CONTRIBUTING.md`, `TODO.md`).

## ✅ Testing

- [ ] Test coverage is effectively zero — the only test ([App.test.js](src/components/App/App.test.js)) just checks the heading renders. (`npm test` couldn't run at all until [vitest.config.js](vitest.config.js) was told this project keeps JSX in `.js` files, and [setupTests.js](src/setupTests.js) got `matchMedia`/`ResizeObserver` mocks and an `IntersectionObserver` mock that actually reports the loader as visible.) Add tests for:
  - [ ] `Pokedex` filtering logic (search, type filter, generation grouping, "clear filters")
  - [x] Favorites add/remove + localStorage persistence — [Pokedex.favorites.test.js](src/components/Pokedex/Pokedex.favorites.test.js): add from a card, toast, Favorites view, remove + Undo, saved favorites from another generation loading without leaking into Browse, and the empty state.
  - [ ] `PokemonDetail` tabs (stats/abilities/evolution/moves/effectiveness) and the mock-data fallback path
- [ ] No CI workflow exists ([.github/](.github/) only has `dependabot.yml`) — add a GitHub Actions workflow to run `npm test` and `npm run build` on PRs.
- [ ] No coverage reporting/thresholds configured in [vitest.config.js](vitest.config.js).

## ⚡ Performance

- [x] `loadGenerationBatch` (formerly `loadPokemons`) fetches each Pokémon's full detail **and** its species data individually per batch (up to 60 requests for a 30-item batch) — was previously uncached. Fixed as part of centralizing the API client ([src/api/pokeapi.js](src/api/pokeapi.js)): every request now goes through an in-memory cache keyed by URL, so re-fetching the same species/Pokémon/type (e.g. revisiting a generation, favoriting something already seen elsewhere, opening the same Pokémon's detail twice) no longer hits the network twice. This is per-session/in-memory only, not persistent across reloads — a persistent cache (e.g. via the service worker or IndexedDB) is still a possible future improvement.
- [x] No `<img>` `width`/`height` attributes on Pokémon sprites/type icons ([Pokedex.js](src/components/Pokedex/Pokedex.js)) — risks layout shift, contradicting the README's claimed CLS < 0.1. Fixed: card sprites (100×100), favorite-chip sprites (36×36) and the type icons in [TypeFilterModal.js](src/components/TypeFilterModal/TypeFilterModal.js) (24×24) now carry `width`/`height` matching their CSS sizes, so space is reserved before the stylesheet loads. The CSS still controls the rendered size (e.g. 72px cards on mobile). The CLS < 0.1 claim itself hasn't been measured.
- [x] Service worker precache list ([public/sw.js](public/sw.js)) was static and didn't include the hashed JS/CSS bundle filenames Vite produces on build — the very first offline visit after install wouldn't have the app shell cached. Fixed with the same `swBuildManifest` plugin in [vite.config.js](vite.config.js) (no Workbox dependency needed): it injects every file under `assets/` (hashed bundles) and `types/` (filter icons) into the precache list of the built `sw.js`. The same work uncovered a second bug: `caches.match` respected `Vary: Origin`, which hosts commonly send, but Vite's `crossorigin` script/link tags add an `Origin` header that the cached requests didn't have — so cached bundles never matched and the app shell never loaded offline. Lookups now use `ignoreVary: true`. Verified against `vite preview`: after install, a fresh offline load renders the full styled app shell. PokéAPI responses are still not cached (the SW only caches same-origin responses), so Pokémon data itself isn't available offline.
- [x] `CACHE_NAME` in [sw.js](public/sw.js#L4) was hardcoded (`pokedex-v1`) with no build-time versioning — every deploy needed someone to remember to bump it manually or stale caches persisted indefinitely. Fixed with a small `swBuildManifest` plugin in [vite.config.js](vite.config.js) that replaces a `__BUILD_HASH__` placeholder in the built `sw.js` with a hash of the rest of the build output. The name only changes when a built file actually changes, and the build fails if the placeholder goes missing. In dev the placeholder stays literal, giving one fixed dev cache name.

## ♿ Accessibility

- [x] Detail modal ([PokemonDetail.js](src/components/PokemonDetail/PokemonDetail.js)) has no focus trap, no focus-return-to-trigger on close, and no `Escape`-to-close handler. — Escape and focus return were already in the shared [Modal.js](src/components/Modal/Modal.js); added a Tab/Shift+Tab focus trap there, so every modal/sheet gets it.
- [x] No visually-hidden "skip to content" link or landmark roles beyond the header. — Added a skip link that appears on keyboard focus, and the content area is now `<main id="main-content">` (alongside the existing `<nav>`s).
- [x] Verify color-only type badges/swatches have sufficient contrast and a non-color cue for colorblind users (icons help, but confirm labels are always visible, not just on hover). — Labels were always visible. Fixed contrast: white badge/pill text failed WCAG AA on 13 of 18 types (Electric 1.49:1); `getTypeTextColor` in [constants.js](src/constants.js) now picks white or near-black per type (min 5.6:1). Type-filter swatches are now solid so the white icons show in light mode, and selected types get a check mark, not just a color change.

## 📱 Mobile

Found by running the built app in headless Chromium emulating an iPhone SE (320px, portrait and landscape).

- [x] **Whole generation loads without scrolling** ([Pokedex.js](src/components/Pokedex/Pokedex.js)) — with zero scrolling, all 151 Gen I cards loaded within ~3s (393 requests, ~2.7 MB). Infinite scroll isn't limiting anything, which costs mobile data/battery on every generation switch. Cause was a separate effect that called `loadGenerationBatch` on every change, so each finished batch kicked off the next. Removed it; the `IntersectionObserver` (now with a 300px `rootMargin`) is the only thing that loads batches.
- [x] **Install prompt never appears on iOS** ([useInstallPrompt.js](src/hooks/useInstallPrompt.js)) — it relies on `beforeinstallprompt`, which Safari (and Firefox) never fire. Show a "Share → Add to Home Screen" hint in Settings on iOS instead. [index.html](index.html) is also missing the `apple-mobile-web-app-*` meta tags (status-bar style, title). — Settings now shows a Share → Add to Home Screen hint on iOS/iPadOS (`showIosHint` from the hook), and index.html has `apple-mobile-web-app-title`, `apple-mobile-web-app-status-bar-style` (`default`, so it follows theme-color) and `mobile-web-app-capable`.
- [x] **Tap targets below 44–48px** — on phones the favorite star is 28×28 ([Pokedex.css](src/components/Pokedex/Pokedex.css), ≤480px rule) and overlaps large sprites like Venusaur/Charizard; the sheet close button is 32×32 ([Modal.css](src/components/Modal/Modal.css)); detail tabs are ~31px tall ([PokemonDetail.css](src/components/PokemonDetail/PokemonDetail.css)). — On phones the star is now a borderless 44×44 target in the card's corner (filled vs outline star shows the state), the close button has an invisible hit area bringing it to 44px, and detail tabs have `min-height: 44px` on mobile.
- [x] **Detail tabs hide off-screen with no cue** — the tab row is ~501px wide in a ~318px sheet with its scrollbar hidden, so Effectiveness/Sprites/Forms aren't discoverable. Add an edge fade/scroll hint, or wrap/shorten the tabs. — The tab row now fades out on whichever edge has more tabs scrolled out of view, and a tapped tab scrolls fully into view.
- [x] **Detail sheet opens mostly empty** — a 96px sprite scaled to 140px sits in a large box that fills about half the first screen before any stats. Consider `sprites.other['official-artwork']` and a more compact header on mobile. — [PokemonDetail](src/components/PokemonDetail/PokemonDetail.js) now defaults to the official artwork, and on mobile the image sits beside the stat total in a short row.
- [x] **Search input not set up for phone keyboards** ([Pokedex.js](src/components/Pokedex/Pokedex.js)) — plain `type="text"`, no `enterKeyHint="search"`, autocorrect/autocapitalize left on (iOS may "correct" Pokémon names), no clear button, and Enter doesn't dismiss the keyboard. — Now a `type="search"` input inside a `role="search"` form with `enterKeyHint="search"`, autocorrect/autocapitalize/spellcheck off and an `aria-label`; a 44px clear button appears when there's text; Enter blurs the input to dismiss the keyboard.
- [x] **Bottom nav may cover results while typing** — on Android the fixed [BottomNav](src/components/BottomNav/BottomNav.css) likely rides up above the on-screen keyboard. Verify on a real device; if so, hide it while the search input is focused. — Hidden on mobile while the search input is focused (CSS `:has()`), whether or not a given browser actually lifts it; harmless where the keyboard just overlays it. Not verified on a real device.
- [x] **Sticky hover effects on touch** — `.tab:hover`, `.theme-option:hover`, `.clear-filters-btn:hover`, the error "Try Again" button aren't wrapped in `@media (hover: hover)`, so they stay stuck after a tap. The detail and type-filter modals already handle this correctly. — Every `:hover` rule (except scrollbar thumbs) is now wrapped in `@media (hover: hover)`. The old `@media (hover: none)` override blocks were removed, since they only reset `transform` and left background/shadow hovers stuck.
- [x] **Status bar flashes black** — [index.html](index.html) and [manifest.json](public/manifest.json) set `theme-color` to `#000000`, so the status bar is black until JS sets the real color, and the installed app's splash screen uses black/white. Add per-scheme `<meta name="theme-color" media="(prefers-color-scheme: …)">` tags and match the manifest colors to the app. — index.html now has light/dark `theme-color` tags matching App.js, which updates both tags so a manual theme choice still wins. The manifest uses the light theme's colors (manifests can't vary by color scheme), so the splash screen is light even on dark-mode devices.
- [ ] **Weak landscape support** — the manifest locks the installed app to `portrait-primary` (hurts tablets/foldables); on 375px-tall landscape screens the title + search take ~40% of the height; and there's no left/right `env(safe-area-inset-*)` padding for the notch despite `viewport-fit=cover`.
- [ ] **Search/filters scroll away** — on mobile you have to tap Browse to jump back to the top to change a search or filter mid-list. Consider a compact sticky header.
- [x] **Type filter lists unused types** ([TypeFilterModal.js](src/components/TypeFilterModal/TypeFilterModal.js)) — `stellar`, `unknown` and `shadow` come back from `/type` but no browsable Pokémon has them, and they cost a whole row on a phone screen. Filter them out — the fetch in [Pokedex.js](src/components/Pokedex/Pokedex.js) now keeps only types listed in `typeColors`.

## 📲 PWA

Goal: an installed app that updates reliably, works offline for anything you've already seen, and behaves like a native app (back button, deep links, share).

**Updates & service worker** ([sw.js](public/sw.js), [registerSW.js](src/registerSW.js))

- [ ] **Updates never take over.** A new deploy installs a new service worker, but with no `skipWaiting()`/`clients.claim()` and no update UI it sits in "waiting" until every window of the app is closed. Installed users can stay on an old version for days. Detect `registration.waiting` / `updatefound`, show an "Update available · Reload" [Toast](src/components/Toast/Toast.js), and on tap `postMessage` the waiting worker to `skipWaiting()` and reload on `controllerchange`. Also check for updates on `visibilitychange`, not only hourly.
- [ ] **Service worker registered in development.** [index.js](src/index.js) always imports `registerSW`, so `vite dev` serves the unbuilt `sw.js` (placeholders never filled in), which caches dev modules cache-first and can serve stale code while developing. Register only when `import.meta.env.PROD`.
- [ ] **Fetch handler intercepts every request.** It calls `respondWith` for non-GET requests too (where `cache.put` throws, unhandled), and for cross-origin requests it can't cache anyway. Return early for non-GET and for origins it doesn't handle, and catch `cache.put` failures. Also remove the leftover `console.log('Opened cache')`.
- [x] **Navigations with a query string miss the cache.** `caches.match` is exact, so `/?action=search` (the manifest shortcut) or any future deep link isn't served from the precached `/` offline. Answer every navigation request with the cached app shell (`ignoreSearch`, or match `/` explicitly). — Every navigation request is now answered with the cached `/`, falling back to the network.

**Offline data** — right now "offline" means the app shell only: every PokéAPI and sprite request goes to the network, so offline you get an empty grid and error screens.

- [x] **Cache PokéAPI responses.** The fetch handler skips them (`response.type !== 'basic'`). PokéAPI data never changes, so serve it cache-first from a separate cache that isn't wiped on each deploy (unlike `CACHE_NAME`), with a cap on entries/size. This makes generations, types, the name index and every Pokémon you've viewed work offline. — Done in [sw.js](public/sw.js): GET requests to `pokeapi.co` go cache-first through a `pokeapi-v1` cache that the activate handler keeps across deploys. Only `/pokemon/` and `/pokemon-species/` entries are evicted (oldest first, beyond 600); list endpoints and the name index are kept so the app can always start offline. Checked with a Node harness against a fake Cache API (miss/hit, offline hit and miss, eviction order, deploy cleanup); not yet tried in a real browser.
- [x] **Cache sprites.** Card and detail images from `raw.githubusercontent.com` are plain `<img>` requests, so they come back as opaque responses. Chrome counts each opaque response as several MB of quota, so either add `crossorigin="anonymous"` to the sprite `<img>`s (GitHub serves CORS headers) or keep the sprite cache small. Cache-first with an entry cap. — Sprite `<img>`s now use `crossOrigin="anonymous"` (confirmed `raw.githubusercontent.com` sends `Access-Control-Allow-Origin: *`), and [sw.js](public/sw.js) caches them cache-first in `sprites-v1` (kept across deploys, oldest evicted beyond 500). Opaque responses are never stored.
- [x] **Keep favorites available offline.** When a Pokémon is favorited, prefetch its data and sprite into the caches above, so the Favorites screen always works offline, not only for Pokémon viewed recently. — [offlineFavorites.js](src/offlineFavorites.js) keeps a `pokedex-favorites-v1` cache in sync with the favorites list (each favorite's data, species, evolution chain, types and sprites; copied from existing caches when possible), run from [useFavorites](src/hooks/useFavorites.js) a second after the list changes. The service worker checks every cache first, and never evicts this one.
- [x] **Offline state in the UI.** Offline failures currently show "404 · Could not load…", which reads as a bug. Watch `navigator.onLine` / the `online`/`offline` events: show a "You're offline — showing saved Pokémon" notice instead of the 404, keep showing whatever is cached, and retry automatically when back online. — [useOnlineStatus](src/hooks/useOnlineStatus.js) drives an offline notice on Browse/Favorites and offline wording in place of the 404s (generation list, next batch, unsaved favorites). Everything that failed retries when the `online` event fires. "Try Again" after a failed batch now carries on from that batch instead of reloading the whole generation.

**Manifest** ([manifest.json](public/manifest.json))

- [x] **Maskable icons.** `logo192`/`logo512` are marked `"any maskable"`. The combined value is discouraged: an icon padded for masking looks too small as "any", and an unpadded one gets cropped as "maskable". Add separate maskable icons with the logo inside the central safe zone (check with maskable.app) and mark the existing ones `"any"`. — Added [maskable-192.png](public/maskable-192.png)/[maskable-512.png](public/maskable-512.png), generated from `logo512.png`: full-bleed coral background (the tile's own color) with the Pokédex drawing scaled to 90%, which keeps it well inside the safe zone (farthest corner 172px from the center vs the 205px safe radius). Checked with circle and squircle crops. The logos are now `"any"` only.
- [x] **Trim the icon list.** `favicon.ico`, the 16/32px PNGs and `apple-touch-icon.png` don't belong in the manifest (browsers use them from `index.html`). Keep 192 and 512 (plus the maskable pair). — Done; the favicons and Apple touch icon are still linked from `index.html`.
- [x] **Add `id`, `lang` and `dir`.** Without `id`, the app's identity is derived from `start_url`, so changing `start_url` later would make browsers treat it as a different app. — Added `"id": "/"` (same as the current `start_url`, so existing installs keep their identity), `"lang": "en"` and `"dir": "ltr"`.
- [ ] **Add screenshots.** `screenshots` is empty. Chrome shows a richer install dialog (Android and desktop) when there are screenshots with `form_factor: "narrow"` and `"wide"`.
- [x] **The "Search" shortcut does nothing.** It opens `/?action=search`, but the app never reads `action`. Handle it (open Browse and focus the search box) or remove it, and consider a "Favorites" shortcut. — `?action=search` now focuses the search box on launch, and there's a Favorites shortcut (`/?view=favorites`).
- [ ] Orientation is locked to `portrait-primary` — see "Weak landscape support" under Mobile.

**Native-app behavior**

- [x] **Android back button exits the app.** Opening the detail sheet, the filter or generation sheet, or switching to Favorites/Settings doesn't add a history entry, so the system back gesture closes the whole installed app instead of the sheet. Push a history entry when a sheet opens or the view changes, and close/go back on `popstate`. — [useHistoryLocation](src/hooks/useHistoryLocation.js) keeps the screen, generation, open Pokémon and open sheet in history entries; back/forward apply them. Closing an overlay with its own button steps back instead of pushing, so the stack stays consistent, and a choice made inside a sheet (a generation) replaces the sheet's entry, so back doesn't reopen it. Covered in [Pokedex.favorites.test.js](src/components/Pokedex/Pokedex.favorites.test.js).
- [x] **Deep links.** Give views and Pokémon their own URLs (e.g. `?view=favorites`, `?pokemon=25`) so reloads restore where you were, shortcuts can target them, and links can be shared. Pairs with the back-button item. — `?view=favorites|settings`, `?gen=generation-iv` and `?pokemon=pikachu` (ids work too and are rewritten to the name). A linked Pokémon that isn't loaded is fetched by name; an unknown one shows a toast. Sheets aren't in the URL.
- [x] **Share a Pokémon.** Add a share button to the detail sheet using `navigator.share()` with the deep link, falling back to copying the link. Depends on deep links. — Share button next to the star in the detail header; cancelling the share sheet does nothing, other failures fall back to copying, with a toast either way.
- [x] **Theme flash on launch.** If the saved theme differs from the OS setting, the app starts in the OS theme until React loads (see "Status bar flashes black" under Mobile). Set `data-theme` and `theme-color` from `localStorage` in a small inline script in [index.html](index.html) before first paint. — Done; App.js notes the two must stay in sync.
- [x] **iOS launch screen.** iOS shows a blank white screen while an installed web app starts. Add `apple-touch-startup-image` links, or at least confirm what current iOS versions show. — Added 20 portrait launch screens (iPhone SE through 16 Pro Max, iPads) in [public/splash/](public/splash/), generated by [scripts/generate-launch-screens.py](scripts/generate-launch-screens.py) (icon on the light background; 400 KB total), plus `apple-mobile-web-app-capable`. Light only: dark variants would need `prefers-color-scheme` in the link `media`, which iOS may not honor (a failing query shows nothing). Not yet checked on an iPhone.

**Verification & docs**

- [ ] **Audit it.** Check installability, the manifest and offline behavior in Chrome DevTools → Application (Manifest, Service Workers, "Offline" checkbox) and with Lighthouse, then test install, update and offline on a real Android phone and iPhone.
- [ ] **README's PWA section is out of date.** It still says to "look for the install prompt at the bottom of the screen" and lists `InstallPrompt/` in the project structure (now the Settings install button and [useInstallPrompt.js](src/hooks/useInstallPrompt.js)). The offline section was updated for PokéAPI caching, but still needs revisiting as sprites and the rest land. Update it as the items above land.

## 📚 Documentation

- [x] README technology versions are stale/inconsistent with `package.json` — rewrote the "Technologies Used" section to pull real version numbers and pointed to `package.json` as the source of truth going forward.
- [x] README claims "Background sync for favorites" under PWA offline support — removed; PWA section now only describes what [sw.js](public/sw.js) actually does.
- [x] README performance numbers (Lighthouse 95+, FCP < 1.5s, bundle < 500KB gzipped) read as aspirational/unverified — removed the unverified "Performance" section entirely rather than publish unmeasured numbers.
- [x] Add a `CONTRIBUTING.md`/issue templates to match the "Contributing" section's ambitions, or fold that section down — added `CONTRIBUTING.md`, bug/feature issue templates, and trimmed the README's "Contributing" section to point to them.
